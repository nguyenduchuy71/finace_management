---
phase: 06-optimize-and-deploy
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - package.json
  - vite.config.ts
  - src/hooks/useTransactions.test.ts
  - src/hooks/useCreditCards.test.ts
  - src/hooks/useDashboardStats.test.ts
  - src/stores/filterStore.test.ts
  - src/stores/chatStore.test.ts
  - src/features/transactions/TransactionList.test.tsx
  - src/features/dashboard/DashboardPage.test.tsx
  - src/features/chatbot/ChatPanel.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Unit test script `npm run test` exists in package.json and runs Vitest"
    - "All utility functions have unit tests: currency.test.ts, dates.test.ts, billingCycle.test.ts all pass"
    - "All hooks have unit tests: useTransactions, useCreditCards, useDashboardStats have test files with ≥70% code coverage"
    - "Integration tests verify state management: Zustand store mutations trigger expected side effects"
    - "Integration tests verify API integration: MSW handlers return correct data, TanStack Query updates correctly"
    - "TransactionList component rendering and filtering tested with React Testing Library"
    - "DashboardPage chart rendering tested (mocked Recharts to avoid canvas issues)"
    - "ChatPanel keyboard shortcut (Ctrl+Shift+K) and message sending tested"
    - "Critical user flows tested: view transactions, filter by date, switch accounts, send chat message"
    - "All tests pass: `npm run test` exits with code 0"
  artifacts:
    - path: "package.json"
      provides: "Test script for Vitest"
      contains: '"test": "vitest"'
    - path: "vite.config.ts"
      provides: "Vitest configuration with jsdom environment"
      contains: "test: { environment: 'jsdom'"
    - path: "src/utils/*.test.ts"
      provides: "Unit tests for utility functions (already exist, verify coverage)"
      min_lines: 30
    - path: "src/hooks/useTransactions.test.ts"
      provides: "Unit tests for useTransactions hook"
      min_lines: 50
    - path: "src/hooks/useCreditCards.test.ts"
      provides: "Unit tests for useCreditCards hook"
      min_lines: 50
    - path: "src/hooks/useDashboardStats.test.ts"
      provides: "Unit tests for useDashboardStats hook"
      min_lines: 50
    - path: "src/stores/filterStore.test.ts"
      provides: "Integration tests for Zustand filter store state and side effects"
      min_lines: 60
    - path: "src/stores/chatStore.test.ts"
      provides: "Integration tests for chat store message history and localStorage"
      min_lines: 60
    - path: "src/features/transactions/TransactionList.test.tsx"
      provides: "Component tests for TransactionList rendering and filtering"
      min_lines: 80
    - path: "src/features/dashboard/DashboardPage.test.tsx"
      provides: "Component tests for DashboardPage with mocked charts"
      min_lines: 60
    - path: "src/features/chatbot/ChatPanel.test.tsx"
      provides: "Component tests for ChatPanel keyboard shortcut and UI interactions"
      min_lines: 60
  key_links:
    - from: "package.json"
      to: "vite.config.ts test config"
      via: "npm run test script runs vitest with vite.config.ts settings"
      pattern: '"test".*vitest'
    - from: "Test files"
      to: "MSW handlers"
      via: "Tests import MSW browser and handlers to mock API responses"
      pattern: "import.*mocks"
    - from: "Hook tests"
      to: "TanStack Query"
      via: "Tests wrap components in QueryClientProvider and call hooks"
      pattern: "QueryClientProvider|useQuery"
    - from: "Component tests"
      to: "Zustand stores"
      via: "Tests verify store mutations trigger expected side effects and re-renders"
      pattern: "useShallow|store\\.set|store\\.getState"

---

<objective>
Build comprehensive test coverage across all layers: utilities, hooks, state management, and components. This includes unit tests for pure functions, integration tests for API and state interactions, and component tests for user-facing features. Target ≥70% code coverage and verified critical user flows.

Purpose: High-quality shipping requires confidence that the app works correctly. Test coverage is the primary signal of correctness before deployment. Testing also prevents regressions when optimizations are applied.

Output:
- package.json — test script configured
- src/hooks/*.test.ts — unit tests for all custom hooks
- src/stores/*.test.ts — integration tests for state management
- src/features/**/*.test.tsx — component tests for key UI features
- All tests passing with ≥70% coverage target
</objective>

<execution_context>
@C:/Users/wtfom/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wtfom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-optimize-and-deploy/06-CONTEXT.md

<!-- Existing test infrastructure -->

From package.json (current):
```json
{
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "jsdom": "^28.1.0",
    "msw": "^2.12.10",
    "vitest": "^4.0.18"
  }
}
```

From vite.config.ts (current test setup):
```typescript
test: {
  environment: 'jsdom',
  globals: true,
}
```

Existing tests (verified 2026-03-04):
- src/utils/currency.test.ts ✓
- src/utils/dates.test.ts ✓
- src/utils/billingCycle.test.ts ✓

Test patterns to follow:
- Vitest `describe` + `it` blocks
- React Testing Library for component tests
- MSW for API mocking (deferred render guard prevents hydration issues)
- Direct Zustand store testing via `store.getState()`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test script and extend Vitest configuration</name>
  <files>package.json, vite.config.ts</files>
  <action>
1. In package.json, add test script in "scripts" section:
```json
"scripts": {
  ...existing scripts,
  "test": "vitest",
  "test:ui": "vitest --ui",
  "test:coverage": "vitest --coverage"
}
```

2. In vite.config.ts, update test config:
```typescript
test: {
  environment: 'jsdom',
  globals: true,
  include: ['src/**/*.test.{ts,tsx}'],
  exclude: ['node_modules', 'dist'],
}
```

3. Verify existing utility tests still run and pass

Rationale: Standard Vitest setup allows all tests to run via npm run test. Configuration matches existing Phase 1 setup.
  </action>
  <verify>
- `npm run test` command exists (check package.json "scripts.test")
- Running `npm run test` starts Vitest in watch mode
- Existing utility tests (currency, dates, billingCycle) run and pass
  </verify>
  <done>Test script configured; Vitest finds all test files; existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for custom hooks (useTransactions, useCreditCards, useDashboardStats)</name>
  <files>src/hooks/useTransactions.test.ts, src/hooks/useCreditCards.test.ts, src/hooks/useDashboardStats.test.ts</files>
  <action>
Create unit tests for three critical data hooks. Each test file should:
- Set up MSW server for beforeAll/afterEach/afterAll lifecycle
- Create a wrapper function with QueryClientProvider and test QueryClient
- Test hook initialization without filters
- Test hook response to filter/store state changes
- Test data structure matches expected types
- Test loading and error states

For each hook:

**useTransactions.test.ts:**
- Test fetches paginated transactions
- Test applies date range filter correctly
- Test applies accountId filter correctly
- Test applies transaction type filter
- Test returns expected PaginatedResponse structure

**useCreditCards.test.ts:**
- Test fetches all credit cards
- Test handles pagination correctly
- Test returns CreditCard[] in expected format
- Test handles empty response

**useDashboardStats.test.ts:**
- Test fetches stats for selected date range
- Test stats include income, expense, category breakdown
- Test updates when dashboardStore date changes
- Test returns DashboardStats structure

Use this pattern:
```typescript
import { describe, it, expect, beforeAll, afterEach, afterAll } from 'vitest'
import { renderHook, waitFor } from '@testing-library/react'
import { QueryClientProvider, QueryClient } from '@tanstack/react-query'
import { server } from '@/mocks/browser'
import { useTransactions } from './useTransactions'

beforeAll(() => server.listen())
afterEach(() => server.resetHandlers())
afterAll(() => server.close())

describe('useTransactions', () => {
  it('should fetch transactions', async () => {
    const queryClient = new QueryClient({ ... })
    const wrapper = ({ children }: any) => (
      <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
    )
    const { result } = renderHook(() => useTransactions(), { wrapper })

    await waitFor(() => expect(result.current.isSuccess).toBe(true))
    expect(Array.isArray(result.current.data?.data)).toBe(true)
  })
})
```

Rationale: Hooks are critical to data flow. Tests verify they correctly integrate with TanStack Query and respond to store changes.
  </action>
  <verify>
- All three test files compile without TypeScript errors
- `npm run test` runs all three files and passes
- Hook tests verify data fetching, filtering, and data structure
  </verify>
  <done>Unit tests created for useTransactions, useCreditCards, useDashboardStats; all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Write integration tests for Zustand stores (filterStore, chatStore)</name>
  <files>src/stores/filterStore.test.ts, src/stores/chatStore.test.ts</files>
  <action>
Create integration tests for two critical Zustand stores. Each test should verify:
- State mutations work correctly
- Side effects (localStorage, etc.) are triggered
- Selectors return correct values
- Reset functions work

**filterStore.test.ts:**
- Test setState updates all filter fields (accountId, cardId, dateFrom, dateTo, type, search)
- Test resetFilters clears all filters
- Test selectors return correct filter values
- Verify no localStorage (filterStore is session-only)

**chatStore.test.ts:**
- Test addMessage adds message to state
- Test deleteMessage removes message by id
- Test clearMessages removes all messages
- Test messages persist to localStorage
- Test messages reload from localStorage on init
- Test toggle chat open/closed state
- Test isLoading state for chat API calls

Use pattern:
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { filterStore } from './filterStore'

describe('filterStore', () => {
  beforeEach(() => {
    filterStore.setState({ /* defaults */ })
    localStorage.clear()
  })

  it('should update account filter', () => {
    filterStore.setState({ accountId: 'acc-1' })
    expect(filterStore.getState().accountId).toBe('acc-1')
  })

  it('should reset all filters', () => {
    filterStore.setState({ accountId: 'acc-1', search: 'test' })
    filterStore.getState().resetFilters()
    expect(filterStore.getState().accountId).toBeNull()
    expect(filterStore.getState().search).toBe('')
  })
})
```

Rationale: Zustand stores manage app state. Tests verify mutations work correctly and side effects (localStorage) are triggered. This is critical to data persistence and UI state.
  </action>
  <verify>
- Both test files compile without errors
- `npm run test` runs both files and passes
- All state mutations and side effects verified
  </verify>
  <done>Integration tests created for filterStore and chatStore; all tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Write component tests for TransactionList, DashboardPage, ChatPanel</name>
  <files>src/features/transactions/TransactionList.test.tsx, src/features/dashboard/DashboardPage.test.tsx, src/features/chatbot/ChatPanel.test.tsx</files>
  <action>
Create component tests for three key UI components. Each test should verify:
- Component renders without errors
- Loading state shows skeleton/spinner
- Error state displays error message
- Empty state displays when no data
- Data renders correctly
- User interactions work (filters, buttons, etc.)

**TransactionList.test.tsx:**
- Test renders loading skeleton initially
- Test renders transaction rows after data loads
- Test shows empty state when no transactions
- Test filter changes trigger re-fetch
- Test pagination works (load more button, etc.)

**DashboardPage.test.tsx:**
- Test renders stat cards (Total Income, Total Expense)
- Test renders chart
- Test date range changes update stats
- Mock Recharts to avoid canvas issues in tests
- Test chart data is updated correctly

**ChatPanel.test.tsx:**
- Test Ctrl+Shift+K opens/closes panel
- Test message input sends message
- Test typing indicator shows while waiting
- Test messages render correctly
- Test on mobile, panel is full-screen
- Test on desktop, panel is side panel

Mock Recharts:
```typescript
vi.mock('recharts', () => ({
  ResponsiveContainer: ({ children }: any) => <div>{children}</div>,
  PieChart: () => <div>PieChart</div>,
  // ... other components
}))
```

Use pattern:
```typescript
import { render, screen, waitFor } from '@testing-library/react'
import { QueryClientProvider } from '@tanstack/react-query'
import { server } from '@/mocks/browser'

beforeAll(() => server.listen())
afterEach(() => server.resetHandlers())
afterAll(() => server.close())

describe('TransactionList', () => {
  it('should render loading state', () => {
    render(
      <QueryClientProvider client={testQueryClient}>
        <TransactionList />
      </QueryClientProvider>
    )
    expect(screen.getByTestId('skeleton')).toBeInTheDocument()
  })

  it('should render transactions after load', async () => {
    render(...)
    await waitFor(() => {
      expect(screen.getByRole('row')).toBeInTheDocument()
    })
  })
})
```

Rationale: Component tests verify the UI works correctly from the user's perspective. Tests ensure rendering, state handling, and user interactions all work before deployment.
  </action>
  <verify>
- All three component test files compile without TypeScript errors
- `npm run test` runs all component tests and passes
- Tests cover loading, error, empty, and success states
- Recharts mocking works (no canvas errors)
  </verify>
  <done>Component tests created for TransactionList, DashboardPage, ChatPanel; all tests pass</done>
</task>

<task type="auto">
  <name>Task 5: Run full test suite and verify coverage</name>
  <files>vite.config.ts (read-only verification)</files>
  <action>
Run the complete test suite and verify coverage:

1. Run `npm run test` and verify:
   - All tests pass (green output, no FAIL)
   - No TypeScript errors
   - Watch mode is active (can press q to quit)

2. Optional: Run `npm run test:coverage` if coverage reporter installed:
   - Check coverage metrics for:
     - Utilities: ≥90% (currency, dates, billingCycle, etc.)
     - Hooks: ≥70% (useTransactions, useCreditCards, useDashboardStats)
     - Stores: ≥80% (filterStore, chatStore, themeStore)
     - Components: ≥60% (TransactionList, DashboardPage, ChatPanel — hard to get high coverage due to UI rendering)

3. Identify any gaps:
   - If a utility has <90% coverage, add missing test cases
   - If a hook has <70% coverage, test additional filter combinations
   - If coverage is too low, prioritize critical paths

4. Update any MSW handlers if tests reveal API contract issues

Rationale: Full test run ensures all tests work together. Coverage metrics identify gaps in testing. Critical paths (hooks, utilities) need higher coverage than UI components.
  </action>
  <verify>
- `npm run test` completes with all tests passing
- Zero test failures in output
- Coverage metrics (if run) show ≥70% for critical paths
  </verify>
  <done>Full test suite runs and passes; coverage verified; ready for production testing</done>
</task>

</tasks>

<verification>
Verify Phase 6 Plan 02 completion:
- [ ] npm run test script exists in package.json
- [ ] vite.config.ts has test configuration
- [ ] All utility tests pass (currency, dates, billingCycle)
- [ ] Hook tests created and passing (useTransactions, useCreditCards, useDashboardStats)
- [ ] Store tests created and passing (filterStore, chatStore)
- [ ] Component tests created and passing (TransactionList, DashboardPage, ChatPanel)
- [ ] Full test suite passes: `npm run test` exits with code 0
- [ ] No TypeScript errors in test files
</verification>

<success_criteria>
Plan 02 is complete when:
1. Test script `npm run test` configured and working
2. All existing utility tests still pass
3. Unit tests created for all custom hooks
4. Integration tests created for Zustand stores
5. Component tests created for key UI features
6. All tests pass with ≥70% code coverage target
7. No TypeScript errors or type issues
8. Full test suite completes in reasonable time (<30 seconds)
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimize-and-deploy/06-02-SUMMARY.md`
</output>
