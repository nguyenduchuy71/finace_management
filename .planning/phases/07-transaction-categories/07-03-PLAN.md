---
phase: 07-transaction-categories
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified: [src/mocks/handlers.ts, src/stores/filterStore.test.ts, src/features/transactions/TransactionList.test.tsx]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
user_setup: []

must_haves:
  truths:
    - "MSW handler filters transactions by category param when provided"
    - "FilterStore tests verify category field and setCategory action"
    - "TransactionList integration tests verify category filtering end-to-end"
    - "All CAT requirements verified via automated test commands"
  artifacts:
    - path: "src/mocks/handlers.ts"
      provides: "Updated MSW handlers to filter by category"
      contains: "category param extraction and filtering logic"
    - path: "src/stores/filterStore.test.ts"
      provides: "Tests for category field in FilterState"
      contains: "setCategory action, resetFilters includes category"
    - path: "src/features/transactions/TransactionList.test.tsx"
      provides: "Integration tests for category filtering end-to-end"
      contains: "category filter changes trigger list update"
  key_links:
    - from: "FilterBar.CategoryFilter"
      to: "MSW handler category param"
      via: "TanStack Query request with params"
    - from: "MSW handler"
      to: "filtered response"
      via: "JavaScript array filter()"
---

<objective>
Complete category feature: update MSW handlers and integration tests to verify the full flow (filter change → query param → MSW filtering → response update).

Purpose: Ensure category filtering works end-to-end and all three requirements (CAT-01, CAT-02, CAT-03) are verified by automated tests.

Output: All tests pass, category feature fully functional, ready for phase verification.
</objective>

<execution_context>
@C:/Users/wtfom/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wtfom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-transaction-categories/07-RESEARCH.md
@.planning/phases/07-transaction-categories/07-01-SUMMARY.md
@.planning/phases/07-transaction-categories/07-02-SUMMARY.md

# Existing patterns
@src/mocks/handlers.ts (MSW handler filtering pattern)
@src/stores/filterStore.test.ts (filter store tests)
@src/features/transactions/TransactionList.test.tsx (integration test pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MSW handlers to filter by category</name>
  <files>src/mocks/handlers.ts</files>
  <action>
Update both MSW handlers in `src/mocks/handlers.ts`:

Step 1 — Bank accounts handler (GET /api/accounts/:accountId/transactions):
- Extract category from query params: const category = url.searchParams.get('category')
- Add filtering logic after txType filter:
  if (category && category !== 'all') {
    allTx = allTx.filter((tx) => tx.category === category)
  }
- Note: tx.category comes from mock fixtures or is classified by front-end logic. Assume mock fixtures have category field populated from classifyTransaction() at fixture creation time.

Step 2 — Credit cards handler (GET /api/credit-cards/:cardId/transactions):
- Apply same category filtering logic after txType filter
- All credit card transactions are expenses, so category filtering applies the same way

Step 3 — Verify mock fixtures:
- Check src/mocks/fixtures/transactions.ts — ensure all transaction objects have category field
- If category is missing, add it by applying classifyTransaction(merchantName) at fixture creation
- Commit pattern: Each mockTransactions entry should have category: classifyTransaction(merchantName)

Example pattern:
```typescript
// In handlers.ts
if (category && category !== 'all') {
  allTx = allTx.filter((tx) => tx.category === category)
}
```

Do NOT hardcode category values in handler; trust that frontend or fixtures have it set.
  </action>
  <verify>
    <automated>npm test -- src/mocks/handlers.test.ts 2>/dev/null || echo "No handlers test — verify with integration tests instead"</automated>
  </verify>
  <done>MSW handlers extract category param and filter allTx by category. Both bank and credit card handlers apply same logic. Mock fixtures have category field populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add category tests to FilterStore test suite</name>
  <files>src/stores/filterStore.test.ts</files>
  <behavior>
    - Test 1: FilterState.category defaults to 'all'
    - Test 2: setCategory('Ăn uống') updates store
    - Test 3: resetFilters() resets category to 'all'
    - Test 4: useFilterParams() selector includes category
    - Test 5: setCategory is idempotent (setting same value twice has same effect)
  </behavior>
  <action>
Update `src/stores/filterStore.test.ts` to include category tests:
- Add describe block: describe('Category Filter', ...)
- Test 1: Check initial state useFilterStore.getState().category === 'all'
- Test 2: Call setCategory('Ăn uống'), verify getState().category === 'Ăn uống'
- Test 3: Call resetFilters(), verify getState().category === 'all'
- Test 4: Use useFilterParams() selector, extract category field, verify it's there
- Test 5: Call setCategory twice with same value, verify no side effects (use spy if needed)
- Use same test framework as existing filterStore tests (Vitest)

Run: npm test -- src/stores/filterStore.test.ts
  </action>
  <verify>
    <automated>npm test -- src/stores/filterStore.test.ts</automated>
  </verify>
  <done>All 5 category-specific tests pass. FilterStore category behavior verified. Existing filterStore tests still pass (no regression).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add integration test for category filtering end-to-end</name>
  <files>src/features/transactions/TransactionList.test.tsx</files>
  <behavior>
    - Test 1: TransactionList renders with default filters (category = 'all')
    - Test 2: Changing category filter updates list to only show transactions of that category
    - Test 3: Filtering by 'Ăn uống' excludes non-food transactions
    - Test 4: Filtering by 'Khác' shows unclassified transactions
    - Test 5: Clearing category filter (back to 'all') shows all transactions
  </behavior>
  <action>
Add integration tests to `src/features/transactions/TransactionList.test.tsx`:
- Import render, screen, fireEvent from React Testing Library
- Import TransactionList, useFilterStore from store
- Create describe block: describe('Category Filtering', ...)
- Test 1: Render TransactionList with default filterStore, verify some transaction rows visible
- Test 2: Call useFilterStore.setState({ category: 'Ăn uống' }), wait for list update, verify only food transactions show
  - Use screen.queryByText() to verify merchants match category (or check category badge count)
- Test 3: Verify non-matching merchants are NOT present (use queryByText with expect to fail)
- Test 4: Set category to 'Khác', verify it shows (test with fixture data that has 'Khác' category)
- Test 5: Reset to 'all', verify original count of transactions returns
- Use waitFor() for async updates if needed

Use per-test QueryClient via createWrapper() pattern (from test-setup.ts).

Run: npm test -- src/features/transactions/TransactionList.test.tsx
  </action>
  <verify>
    <automated>npm test -- src/features/transactions/TransactionList.test.tsx</automated>
  </verify>
  <done>All 5 integration tests pass. End-to-end category filtering verified. List updates when filter changes. Existing TransactionList tests still pass (no regression).</done>
</task>

</tasks>

<verification>
After Task 1-3 complete:
1. Run all category-related tests:
   npm test -- src/stores/filterStore.test.ts src/features/transactions/TransactionList.test.tsx
2. Full test suite: npm test
3. Verify no TypeScript errors: npx tsc --noEmit
4. Manual verification (if able to run browser):
   - Navigate to transaction list
   - Change category filter
   - Verify transaction list updates in real-time
   - Click category badge to override
   - Verify override persists on page reload

**Phase Verification Checklist:**

For each requirement, verify automated test exists:

- [ ] CAT-01: classifyTransaction() unit test passes (src/utils/categories.test.ts)
- [ ] CAT-01: TransactionRow renders CategoryBadge test passes (src/features/transactions/TransactionRow.test.ts)
- [ ] CAT-02: categoryOverrideStore persists test passes (src/stores/categoryOverrideStore.test.ts)
- [ ] CAT-02: getEffectiveCategory() merges override test passes (src/stores/categoryOverrideStore.test.ts)
- [ ] CAT-03: FilterBar includes CategoryFilter test passes (src/components/filters/FilterBar.test.ts)
- [ ] CAT-03: Category filter triggers refetch test passes (src/features/transactions/TransactionList.test.tsx)

All 6 requirements verified by automated tests.
</verification>

<success_criteria>
- MSW handlers filter by category param (both bank and CC handlers)
- Mock fixtures have category field populated (not null/undefined)
- FilterStore category tests pass (5 tests)
- TransactionList integration tests pass (5 tests)
- Full test suite passes: npm test
- No TypeScript errors
- No regressions in existing tests
- CAT-01, CAT-02, CAT-03 all verified via automated tests
- Phase 7 complete and ready for verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-transaction-categories/07-03-SUMMARY.md`

Phase 7 is complete. All 3 plans (01, 02, 03) have executed successfully.
</output>
