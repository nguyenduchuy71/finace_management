---
phase: 07-transaction-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/categories.ts, src/utils/categories.ts, src/utils/categories.test.ts, src/stores/categoryOverrideStore.ts, src/stores/categoryOverrideStore.test.ts, src/components/ui/CategoryBadge.tsx]
autonomous: true
requirements: [CAT-01, CAT-02]
user_setup: []

must_haves:
  truths:
    - "classifyTransaction() deterministically maps merchant name to Vietnamese category"
    - "Category override store persists overrides to localStorage"
    - "getEffectiveCategory() merges server category with user override"
    - "CategoryBadge component renders category with semantic color coding"
  artifacts:
    - path: "src/types/categories.ts"
      provides: "Category type definition (6 Vietnamese labels)"
      exports: ["Category"]
    - path: "src/utils/categories.ts"
      provides: "Classification function and taxonomy lookup table"
      exports: ["classifyTransaction", "CATEGORY_TAXONOMY", "getCategoryLabel"]
    - path: "src/utils/categories.test.ts"
      provides: "Unit tests for classification algorithm"
      contains: "keyword matching, fallback behavior"
    - path: "src/stores/categoryOverrideStore.ts"
      provides: "localStorage-backed Zustand store for category overrides"
      exports: ["useCategoryOverrideStore"]
    - path: "src/stores/categoryOverrideStore.test.ts"
      provides: "Tests for store persistence and getEffectiveCategory"
      contains: "localStorage round-trip, override precedence"
    - path: "src/components/ui/CategoryBadge.tsx"
      provides: "Reusable badge component with category color map"
      exports: ["CategoryBadge"]
  key_links:
    - from: "classifyTransaction()"
      to: "CategoryBadge component"
      via: "passed as prop from parent"
    - from: "categoryOverrideStore.setOverride()"
      to: "localStorage"
      via: "automatic JSON serialization in set() mutation"
    - from: "getEffectiveCategory()"
      to: "TransactionRow display (Plan 2)"
      via: "hook selector on categoryOverrideStore"
---

<objective>
Build core category infrastructure: classification function, localStorage override store, and badge component.

Purpose: Establish the deterministic classification algorithm and override persistence layer before integrating into transaction rows and filters.

Output: Utilities, types, stores, and UI components that other tasks depend on. Tests passing.
</objective>

<execution_context>
@C:/Users/wtfom/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wtfom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-transaction-categories/07-RESEARCH.md

# Existing patterns
@src/stores/themeStore.ts (localStorage + Zustand pattern)
@src/types/account.ts (Transaction schema with optional category)
@src/utils/currency.ts (utility module pattern with tests)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Category type and classification utilities</name>
  <files>src/types/categories.ts, src/utils/categories.ts, src/utils/categories.test.ts</files>
  <behavior>
    - Test 1: classifyTransaction('Grab') returns 'Di chuyển'
    - Test 2: classifyTransaction('Shopee') returns 'Mua sắm'
    - Test 3: classifyTransaction('Circle K') returns 'Ăn uống'
    - Test 4: classifyTransaction('Electricity Bill') returns 'Hóa đơn'
    - Test 5: classifyTransaction('Unknown Merchant') returns 'Khác'
    - Test 6: classifyTransaction(undefined) returns 'Khác'
    - Test 7: getCategoryLabel('Ăn uống') returns 'Ăn uống'
  </behavior>
  <action>
Create `src/types/categories.ts` with Category type (union of 6 Vietnamese category strings: Ăn uống, Mua sắm, Di chuyển, Giải trí, Hóa đơn, Khác).

Create `src/utils/categories.ts` with:
- CATEGORY_TAXONOMY constant: Record<Category, string[]> mapping each category to lowercase keyword list (from research: Circle K, Highlands Coffee, Grab, Shopee, Lazada, Netflix, Spotify, Electricity, etc.)
- classifyTransaction(merchant: string | undefined): Category — loop through CATEGORY_TAXONOMY, return first match on keywords.includes(), fallback to 'Khác'
- getCategoryLabel(category: Category): string — return category label (in this case, same as key since Vietnamese names are labels)

Create `src/utils/categories.test.ts` using Vitest:
- Import classifyTransaction, CATEGORY_TAXONOMY
- Test each category via keyword examples
- Test fallback (undefined merchant, unmatched merchant)
- Use describe('Category Classification', ...) block, 7 test cases total
- Run: npm test -- src/utils/categories.test.ts
  </action>
  <verify>
    <automated>npm test -- src/utils/categories.test.ts</automated>
  </verify>
  <done>All 7 classification tests pass. Type exports verified. getCategoryLabel function works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create categoryOverrideStore with localStorage persistence</name>
  <files>src/stores/categoryOverrideStore.ts, src/stores/categoryOverrideStore.test.ts</files>
  <behavior>
    - Test 1: Initial overrides Map is empty
    - Test 2: setOverride(txId, category) adds to Map
    - Test 3: getEffectiveCategory(txId, serverCategory) returns override if set
    - Test 4: getEffectiveCategory(txId, serverCategory) returns serverCategory if no override
    - Test 5: setOverride() persists to localStorage as JSON array
    - Test 6: Store initializes from localStorage on creation
    - Test 7: clearOverride(txId) removes from Map and updates localStorage
  </behavior>
  <action>
Create `src/stores/categoryOverrideStore.ts` following themeStore pattern:
- Interface CategoryOverrideState with: overrides: Map<string, Category>, setOverride(txId, category), clearOverride(txId), getEffectiveCategory(txId, serverCategory)
- Initialize by reading localStorage key 'finance-category-overrides' — deserialize JSON array to Map
- setOverride: create new Map, add entry, JSON.stringify([...newMap]), save to localStorage, update state
- clearOverride: same pattern, delete entry from Map
- getEffectiveCategory: use get().overrides.get(txId) ?? serverCategory
- Apply immediately on module load (not in useEffect)

Create `src/stores/categoryOverrideStore.test.ts`:
- Use createWrapper() pattern if needed (per test-setup.ts)
- Test localStorage round-trip: set override, reload from localStorage, verify it's there
- Test precedence: override shadows server category
- Test deletion: clearOverride removes from both Map and localStorage
- Run: npm test -- src/stores/categoryOverrideStore.test.ts
  </action>
  <verify>
    <automated>npm test -- src/stores/categoryOverrideStore.test.ts</automated>
  </verify>
  <done>All 7 store tests pass. localStorage round-trip verified. Override precedence correct.</done>
</task>

<task type="auto">
  <name>Task 3: Create CategoryBadge component with color map</name>
  <files>src/components/ui/CategoryBadge.tsx</files>
  <action>
Create `src/components/ui/CategoryBadge.tsx` component:
- Import Badge from '@/components/ui/badge', type Category from '@/types/categories'
- Props: category: Category, className?: string
- Define categoryColorMap: Record<Category, string> with semantic colors per category (from research: Ăn uống = orange, Mua sắm = pink, Di chuyển = blue, Giải trí = purple, Hóa đơn = amber, Khác = slate, all with light + dark mode variants)
- Return: <Badge variant="secondary" className={`${categoryColorMap[category]} ${className}`}>{category}</Badge>
- Example color: 'Ăn uống' -> 'bg-orange-100 text-orange-700 dark:bg-orange-900/40 dark:text-orange-400'
  </action>
  <verify>
npm test -- src/components/ui/CategoryBadge.tsx
  </verify>
  <done>Component renders without errors. Color classes match category. Badge props work.</done>
</task>

</tasks>

<verification>
After Task 1-3 complete:
1. Run full test suite: npm test
2. Verify no TypeScript errors: npx tsc --noEmit
3. Verify exports: grep "export" src/types/categories.ts src/utils/categories.ts src/stores/categoryOverrideStore.ts src/components/ui/CategoryBadge.tsx
4. Confirm localStorage key matches expected value: grep "finance-category-overrides" src/stores/categoryOverrideStore.ts
</verification>

<success_criteria>
- classifyTransaction() works on all test cases (deterministic, no external deps)
- categoryOverrideStore persists to localStorage (Map serialization correct)
- getEffectiveCategory() merges override + server category correctly
- CategoryBadge renders with semantic color coding (6 colors, light/dark modes)
- All tests pass: 7 classification + 7 store + component render = 14 total
- No TypeScript errors
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-transaction-categories/07-01-SUMMARY.md`
</output>
