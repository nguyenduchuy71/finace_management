---
phase: 08-budget-tracking
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/components/budget/BudgetSettings.tsx, src/components/budget/BudgetSettings.test.ts, src/features/dashboard/BudgetProgressSection.tsx, src/pages/DashboardPage.tsx]
autonomous: false
requirements: [BUDGET-01, BUDGET-02, BUDGET-03]
user_setup: []

must_haves:
  truths:
    - "User can open BudgetSettings dialog, enter monthly budgets for all 6 categories in VND currency format, and save"
    - "Dashboard displays BudgetProgressSection below stat cards, showing progress bars only for categories with budgets > 0"
    - "BudgetProgressSection calculates spent from categoryBreakdown and budgets from useBudgetStore, renders bars with conditional colors"
    - "Settings dialog validates input, parses VND currency format (thousand separators), persists to budgetStore"
  artifacts:
    - path: "src/components/budget/BudgetSettings.tsx"
      provides: "Dialog UI to set monthly budgets per category"
      exports: ["BudgetSettings"]
    - path: "src/components/budget/BudgetSettings.test.ts"
      provides: "Integration tests for settings dialog: open/close, input parsing, persistence"
      contains: "dialog open/close, currency input parsing with parseVND, setBudget calls"
    - path: "src/features/dashboard/BudgetProgressSection.tsx"
      provides: "Container component that maps categoryBreakdown + budgets to BudgetProgressBar rows"
      exports: ["BudgetProgressSection"]
    - path: "src/pages/DashboardPage.tsx"
      provides: "MODIFIED: Add <BudgetProgressSection> after stat cards, pass categoryBreakdown prop"
      modifies: "existing grid layout to include budget section"
  key_links:
    - from: "BudgetSettings dialog"
      to: "useBudgetStore.setBudget()"
      via: "handleSave callback collects inputs and calls setBudget per category"
    - from: "BudgetProgressSection"
      to: "BudgetProgressBar"
      via: "map over budgets, match with categoryBreakdown, pass spent/budget to child"
    - from: "DashboardPage.tsx"
      to: "BudgetProgressSection"
      via: "pass data.categoryBreakdown prop after stat cards"
---

<objective>
Integrate budget settings UI and dashboard budget progress display.

Purpose: Complete BUDGET-01 (user sets budgets), BUDGET-02 (dashboard shows progress), and BUDGET-03 (conditional styling) by wiring together settings dialog and dashboard progress section.

Output: Fully functional budget tracking feature — users can set budgets and see spending progress on dashboard.
</objective>

<execution_context>
@C:/Users/wtfom/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wtfom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-budget-tracking/08-RESEARCH.md

# Prior plan output
@.planning/phases/08-budget-tracking/08-01-SUMMARY.md (budgetStore, BudgetProgressBar)

# Existing patterns
@src/features/chat/ChatSettings.tsx (Dialog component pattern with open/close state wiring)
@src/features/dashboard/DashboardPage.tsx (dashboard layout — stat cards grid)
@src/utils/currency.ts (formatVND, parseVND utilities)
@src/types/categories.ts (Category type, 6 categories)
@src/services/dashboard.ts (CategoryBreakdownItem interface with category and amount fields)
</context>

<interfaces>
From Plan 08-01 (BudgetProgressBar, useBudgetStore):

```typescript
// src/components/budget/BudgetProgressBar.tsx
interface BudgetProgressBarProps {
  category: Category
  spent: number
  budget: number
  className?: string
}

// src/stores/budgetStore.ts
export const useBudgetStore = create<BudgetState>(...)
interface BudgetState {
  budgets: Record<Category, number>
  setBudget: (category: Category, amount: number) => void
  clearBudget: (category: Category) => void
  getBudget: (category: Category) => number
}
```

From existing code (DashboardPage.tsx, dashboard service):

```typescript
// src/services/dashboard.ts
export interface CategoryBreakdownItem {
  category: Category
  amount: number
}

export interface DashboardStats {
  totalIncome: number
  totalExpense: number
  transactionCount: number
  bankIncome: number
  bankExpense: number
  ccIncome: number
  ccExpense: number
  categoryBreakdown: CategoryBreakdownItem[]
}

// src/pages/DashboardPage.tsx
const { data, isLoading, isError, refetch } = useDashboardStats()
// data.categoryBreakdown is available for passing to BudgetProgressSection
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BudgetSettings dialog component</name>
  <files>src/components/budget/BudgetSettings.tsx, src/components/budget/BudgetSettings.test.ts</files>
  <behavior>
    - Test 1: Dialog opens with controlled open prop, renders 6 category inputs (one per category)
    - Test 2: Each category input shows label and text field with formatVND-formatted initial budget value
    - Test 3: Clicking "Lưu" (Save) button calls setBudget for each category with parseVND(input) parsed amount
    - Test 4: User can type "100.000" (VND format with thousand separator) → parseVND extracts 100000 integer
    - Test 5: Changing budget from "1.000.000" to "500.000" → setBudget called with 500000
    - Test 6: Clicking outside dialog or pressing Escape closes dialog (controlled via onOpenChange callback)
    - Test 7: Re-opening dialog shows previously saved budgets in input fields (loads from useBudgetStore)
    - Test 8: Saving empty input (or "0") → setBudget(category, 0) called (clears budget for category if was set)
  </behavior>
  <action>
Create `src/components/budget/BudgetSettings.tsx`:
- Props: open (boolean), onOpenChange ((open: boolean) => void)
- Use shadcn Dialog component (controlled via open prop, onOpenChange callback)
- DialogHeader with title "Đặt ngân sách theo danh mục" (Set budgets by category)
- Form: iterate over all 6 categories from CATEGORY_TAXONOMY or Object.keys(CATEGORY_TAXONOMY) as Category[]
- For each category:
  - Label: category name (e.g., "Ăn uống")
  - Input: text type (not number — allows thousand-separator input like "100.000")
  - Initial value: formatVND(useBudgetStore().getBudget(category)) to display current budget with separators
  - onChange: update local state (controlled input)
- handleSave:
  - Loop through categories
  - For each: amount = parseVND(localBudgets[category])
  - If amount > 0: call setBudget(category, amount)
  - If amount === 0 or empty: call clearBudget(category) or just don't call setBudget (results in budget=0)
  - After save: onOpenChange(false) to close dialog
- DialogFooter: "Lưu" button triggers handleSave, optional Cancel button
- Match ChatSettings pattern for Dialog control (see Phase 5)
- Import: Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from '@/components/ui/dialog'
- Import: Button, Input from '@/components/ui'
- Import: useBudgetStore, formatVND, parseVND, CATEGORY_TAXONOMY

Create `src/components/budget/BudgetSettings.test.ts` using Vitest + React Testing Library:
- Describe block: 'BudgetSettings Dialog Component'
- Test suite: dialog open/close, input rendering, currency parsing, persistence, re-open shows saved values
- 8 test cases
- Use render() with mock functions for onOpenChange, test userEvent.click() for Save button
- Assert that setBudget is called with correct parsed amount (use jest.spyOn if needed, or verify via store state)
- Test parseVND edge cases: "100.000" → 100000, "1.500.000" → 1500000, "0" → 0, empty string → 0
  </action>
  <verify>
    <automated>npm test -- src/components/budget/BudgetSettings.test.ts</automated>
  </verify>
  <done>All 8 tests passing. Dialog opens/closes via controlled prop. Inputs parse VND currency format with thousand separators. Save button calls setBudget for all categories. Settings persist and re-appear on next open.</done>
</task>

<task type="auto">
  <name>Task 2: Create BudgetProgressSection component and integrate into dashboard</name>
  <files>src/features/dashboard/BudgetProgressSection.tsx, src/pages/DashboardPage.tsx</files>
  <action>
Create `src/features/dashboard/BudgetProgressSection.tsx`:
- Props: categoryBreakdown (CategoryBreakdownItem[])
- Hook: const { budgets } = useBudgetStore() — get all budgets
- Guard: const hasBudgets = Object.values(budgets).some(b => b > 0) — if no budgets set, return null (entire section hidden)
- Build categoryMap: Object.fromEntries(categoryBreakdown.map(item => [item.category, item.amount])) — lookup spent per category
- Render:
  - Card wrapper (shadcn Card)
  - CardHeader with title "Tiến độ ngân sách" (Budget Progress) in small gray text (match StatCard style)
  - CardContent: space-y-4 div (vertical gap)
  - Map over Object.keys(budgets) as Category[]:
    - For each category: render <BudgetProgressBar category={category} spent={categoryMap[category] ?? 0} budget={budgets[category]} />
    - BudgetProgressBar internally hides if budget === 0 (so only bars with budgets > 0 appear)
- Styling: match Card layout from DashboardPage, consistent padding/spacing
- Import: Card, CardContent, CardHeader, CardTitle from '@/components/ui/card'
- Import: useBudgetStore, BudgetProgressBar, CategoryBreakdownItem type

**NOTE:** Settings dialog trigger (e.g., a "Cài đặt ngân sách" button in the dashboard header or AppHeader) is deferred to Phase 8 follow-up work. For now, the BudgetSettings dialog component is built and tested, but not wired to a UI button. Manual testing will use devtools or a future button implementation.

Modify `src/pages/DashboardPage.tsx`:
- After the existing stat cards grid (line ~87, after </div> closing the grid), add:
  ```jsx
  {/* Budget Progress Section — appears after stat cards when budgets are set */}
  {data && <BudgetProgressSection categoryBreakdown={data.categoryBreakdown} />}
  ```
- Import BudgetProgressSection from '@/features/dashboard/BudgetProgressSection'
- The conditional rendering (data &&) ensures section only renders when data is loaded (matches skeleton pattern for stat cards)
- This places budget section in natural reading order: stat cards → category chart → budget progress (visible when scrolling down on mobile)
- Alternative: could place inside the grid for 4th column, but research suggests below stat cards is cleaner UX

Integration points verified:
- categoryBreakdown already available from useDashboardStats() in DashboardPage
- BudgetProgressSection handles guard: if no budgets set, returns null (no empty card shown)
- BudgetProgressBar handles conditional rendering (hidden if budget=0)
- No new API calls needed; uses existing categoryBreakdown data
  </action>
  <verify>
    <automated>npm test -- src/pages/DashboardPage.test.ts -t "budget"</automated>
  </verify>
  <done>BudgetProgressSection integrates into DashboardPage. Component hides when no budgets set. When budgets exist, displays progress bars for all budgeted categories below stat cards. Dashboard renders without errors or type issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete budget feature (settings + dashboard progress)</name>
  <action>
Manually verify the complete budget tracking feature integration:

1. Navigate to Dashboard page in running app (npm run dev)
2. Verify budget progress section does NOT appear initially (no budgets set yet)
3. To set budgets and test (settings dialog button is deferred to Phase 8 follow-up):
   - Open browser devtools console and run:
     ```javascript
     import { useBudgetStore } from './src/stores/budgetStore'
     const store = useBudgetStore.getState()
     store.setBudget('Ăn uống', 5000000)
     store.setBudget('Mua sắm', 3000000)
     ```
   - Then refresh the page
4. After setting budgets, dashboard should display "Tiến độ ngân sách" card with progress bars
5. For each budgeted category, verify:
   - Bar displays category name, percentage, and VND amounts
   - Color coding: emerald (normal), yellow (≥80%), red (≥100%)
6. Refresh page and verify budgets persist and section re-appears
7. Run `npm test` to ensure all tests pass (no regressions)
  </action>
  <verify>
Confirm the following observable behaviors:
- Budget section hidden when no budgets set
- Progress bars appear after setting budgets
- Colors change correctly based on spent/budget ratio
- Data persists across page refresh
- All tests pass: `npm test -- src/components/budget/BudgetSettings.test.ts` and `npm test` full suite
  </verify>
  <done>Budget feature fully integrated. User can set monthly budgets per category and see spending progress on dashboard with conditional color warnings. All tests passing, no regressions, localStorage persistence verified.</done>
  <files>src/components/budget/BudgetSettings.tsx, src/features/dashboard/BudgetProgressSection.tsx, src/pages/DashboardPage.tsx</files>
</task>

</tasks>

<verification>
After task completion:
1. Run `npm test -- src/components/budget/BudgetSettings.test.ts` — 8 tests passing
2. Run `npm test` — full suite passing, no regressions in dashboard tests
3. Dashboard page renders: `npm run dev` and navigate to /dashboard — no errors
4. Manual verification (checkpoint): Settings dialog, budget persistence, progress bars with conditional colors
5. Type check: `npm run type-check` passes (no TypeScript errors)
6. Verify integration:
   - Open devtools > Application > localStorage
   - Set a budget via settings dialog (or devtools workaround)
   - Confirm "finance-budgets" key exists with valid JSON
   - Refresh page — budgets persist
   - Progress bars appear on dashboard with correct colors
</verification>

<success_criteria>
- All tests passing (8 BudgetSettings tests + existing dashboard tests)
- BudgetSettings dialog opens, accepts currency input with thousand separators, saves to budgetStore
- BudgetProgressSection renders below stat cards on dashboard when budgets exist
- Progress bars show correct conditional colors (emerald/yellow/red) based on spent/budget ratio
- Budgets persist to localStorage and survive page refresh
- No regressions in existing dashboard functionality
- Complete feature addresses BUDGET-01 (set budgets), BUDGET-02 (show progress), BUDGET-03 (conditional warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/08-budget-tracking/08-02-SUMMARY.md`
</output>
