---
wave: 3
depends_on: [02-01-PLAN.md, 02-02-PLAN.md, 02-03-PLAN.md]
requirements: [BANK-03, FILTER-02]
files_modified:
  - src/features/accounts/AccountTabs.tsx
  - src/features/creditCards/CreditCardTabs.tsx
  - src/pages/BankAccountsPage.tsx
  - src/pages/CreditCardsPage.tsx
autonomous: true
---

# Plan 02-04: Bank & Credit Card Pages with Account Switching

## Goal

Complete the BankAccountsPage and CreditCardsPage by replacing their stub content with real components: AccountTabs (Tabs-based account switcher), CreditCardTabs (card switcher), FilterBar, and the respective transaction lists. This delivers account switching (BANK-03, FILTER-02) and brings both pages to fully functional state. The full transaction viewing experience is end-to-end working after this plan.

## Context

Plan 02-02 created stub pages with placeholder content. Plan 02-03 created all the transaction list and filter components. This plan wires them together: creates AccountTabs and CreditCardTabs components, then replaces stub pages with real composed layouts. The Zustand `setAccountId`/`setCardId` actions are called when tabs change. Filter state (dateFrom, dateTo, searchQuery, txType) persists across account switches because it lives in the shared Zustand store.

## Tasks

<task id="02-04-T01" name="Create AccountTabs component (BANK-03, FILTER-02)">

Create `src/features/accounts/AccountTabs.tsx`. Renders shadcn Tabs for each bank account. On tab change, calls `useFilterStore.setAccountId`. When accounts load, initializes `accountId` to the first account if not already set. Satisfies BANK-03 and FILTER-02.

```tsx
import { useEffect } from 'react'
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { useFilterStore } from '@/stores/filterStore'
import { useAccounts } from '@/hooks/useAccounts'
import { Skeleton } from '@/components/ui/skeleton'
import type { BankAccount } from '@/types/account'

interface AccountTabsProps {
  onAccountChange?: (accountId: string) => void
}

export function AccountTabs({ onAccountChange }: AccountTabsProps) {
  const accountId = useFilterStore((s) => s.accountId)
  const setAccountId = useFilterStore((s) => s.setAccountId)
  const { data, isLoading } = useAccounts()

  const accounts: BankAccount[] = data?.data ?? []

  // Initialize to first account when accounts load and no account is selected
  useEffect(() => {
    if (accounts.length > 0 && !accountId) {
      const firstId = accounts[0].id
      setAccountId(firstId)
      onAccountChange?.(firstId)
    }
  }, [accounts, accountId, setAccountId, onAccountChange])

  if (isLoading) {
    return (
      <div className="flex gap-2">
        <Skeleton className="h-10 w-32 rounded-md" />
        <Skeleton className="h-10 w-32 rounded-md" />
      </div>
    )
  }

  if (accounts.length === 0) return null

  function handleChange(value: string) {
    setAccountId(value)
    onAccountChange?.(value)
  }

  return (
    <Tabs value={accountId ?? accounts[0].id} onValueChange={handleChange}>
      <TabsList className="h-auto flex-wrap">
        {accounts.map((account) => (
          <TabsTrigger
            key={account.id}
            value={account.id}
            className="min-h-[44px] text-sm"
          >
            <span className="font-medium">{account.bankName}</span>
            <span className="ml-1.5 text-xs opacity-70">{account.accountNumber.slice(-4)}</span>
          </TabsTrigger>
        ))}
      </TabsList>
    </Tabs>
  )
}
```

Note: `BankAccount` type — read `src/types/account.ts` to confirm the field names (`id`, `bankName`, `accountNumber`). Adjust if they differ.

</task>

<task id="02-04-T02" name="Create CreditCardTabs component (FILTER-02)">

Create `src/features/creditCards/CreditCardTabs.tsx`. Analogous to AccountTabs but for credit cards. Calls `useFilterStore.setCardId` on tab change. Initializes to first card when cards load. Satisfies FILTER-02 for the credit card page.

Read `src/types/creditCard.ts` to confirm the CreditCard type fields (id, cardName or name, lastFour or cardNumber) before writing — adjust field references accordingly.

```tsx
import { useEffect } from 'react'
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { useFilterStore } from '@/stores/filterStore'
import { useCreditCards } from '@/hooks/useCreditCards'
import { Skeleton } from '@/components/ui/skeleton'
import type { CreditCard } from '@/types/creditCard'

export function CreditCardTabs() {
  const cardId = useFilterStore((s) => s.cardId)
  const setCardId = useFilterStore((s) => s.setCardId)
  const { data, isLoading } = useCreditCards()

  const cards: CreditCard[] = data?.data ?? []

  // Initialize to first card when cards load and no card is selected
  useEffect(() => {
    if (cards.length > 0 && !cardId) {
      setCardId(cards[0].id)
    }
  }, [cards, cardId, setCardId])

  if (isLoading) {
    return (
      <div className="flex gap-2">
        <Skeleton className="h-10 w-36 rounded-md" />
        <Skeleton className="h-10 w-36 rounded-md" />
      </div>
    )
  }

  if (cards.length === 0) return null

  return (
    <Tabs value={cardId ?? cards[0].id} onValueChange={setCardId}>
      <TabsList className="h-auto flex-wrap">
        {cards.map((card) => (
          <TabsTrigger
            key={card.id}
            value={card.id}
            className="min-h-[44px] text-sm"
          >
            {/* Use the actual field names from CreditCard type — read the type file first */}
            <span className="font-medium">{card.cardName ?? card.id}</span>
          </TabsTrigger>
        ))}
      </TabsList>
    </Tabs>
  )
}
```

IMPORTANT: Before writing this file, read `src/types/creditCard.ts` to get the exact field names for `CreditCard`. Replace `card.cardName ?? card.id` with the actual field from the type definition.

</task>

<task id="02-04-T03" name="Complete BankAccountsPage with all components">

Replace the stub `src/pages/BankAccountsPage.tsx` with the full composed page. The page shows: page title, AccountTabs for account switching, FilterBar for filtering, and TransactionList for the transaction data.

Read the current file first, then replace entirely:

```tsx
import { AccountTabs } from '@/features/accounts/AccountTabs'
import { TransactionList } from '@/features/transactions/TransactionList'
import { FilterBar } from '@/components/filters/FilterBar'
import { Building2 } from 'lucide-react'

export function BankAccountsPage() {
  return (
    <div className="space-y-4">
      {/* Page header */}
      <div className="flex items-center gap-2">
        <Building2 className="h-6 w-6 text-primary" />
        <h1 className="text-2xl font-bold tracking-tight">Tài khoản ngân hàng</h1>
      </div>

      {/* Account switcher — sets accountId in Zustand filter store */}
      <AccountTabs />

      {/* Filter bar — search, date range, transaction type */}
      <FilterBar />

      {/* Transaction list — reads accountId + filters from Zustand, uses useInfiniteQuery */}
      <TransactionList />
    </div>
  )
}
```

</task>

<task id="02-04-T04" name="Complete CreditCardsPage with all components">

Replace the stub `src/pages/CreditCardsPage.tsx` with the full composed page. The page shows: page title, CreditCardTabs for card switching, FilterBar for filtering, and CreditCardTransactionList.

Read the current file first, then replace entirely:

```tsx
import { CreditCardTabs } from '@/features/creditCards/CreditCardTabs'
import { CreditCardTransactionList } from '@/features/creditCards/CreditCardTransactionList'
import { FilterBar } from '@/components/filters/FilterBar'
import { CreditCard } from 'lucide-react'

export function CreditCardsPage() {
  return (
    <div className="space-y-4">
      {/* Page header */}
      <div className="flex items-center gap-2">
        <CreditCard className="h-6 w-6 text-primary" />
        <h1 className="text-2xl font-bold tracking-tight">Thẻ tín dụng</h1>
      </div>

      {/* Card switcher — sets cardId in Zustand filter store */}
      <CreditCardTabs />

      {/* Filter bar — search, date range, transaction type */}
      <FilterBar />

      {/* Credit card transaction list — reads cardId + filters from Zustand */}
      <CreditCardTransactionList />
    </div>
  )
}
```

</task>

## Verification

```bash
cd "D:/Vibe Coding/finace_management"

# Account/Card tab components
test -f src/features/accounts/AccountTabs.tsx && echo "AccountTabs: OK" || echo "AccountTabs: MISSING"
test -f src/features/creditCards/CreditCardTabs.tsx && echo "CreditCardTabs: OK" || echo "CreditCardTabs: MISSING"

# AccountTabs Zustand wiring
grep -q "setAccountId" src/features/accounts/AccountTabs.tsx && echo "AccountTabs: setAccountId OK" || echo "AccountTabs: MISSING setAccountId"
grep -q "useAccounts" src/features/accounts/AccountTabs.tsx && echo "AccountTabs: useAccounts OK" || echo "AccountTabs: MISSING useAccounts"
grep -q "Tabs\|TabsList\|TabsTrigger" src/features/accounts/AccountTabs.tsx && echo "AccountTabs: shadcn Tabs OK" || echo "AccountTabs: MISSING shadcn Tabs"

# CreditCardTabs Zustand wiring
grep -q "setCardId" src/features/creditCards/CreditCardTabs.tsx && echo "CreditCardTabs: setCardId OK" || echo "CreditCardTabs: MISSING setCardId"
grep -q "useCreditCards" src/features/creditCards/CreditCardTabs.tsx && echo "CreditCardTabs: useCreditCards OK" || echo "CreditCardTabs: MISSING useCreditCards"

# Pages no longer have stub content
grep -vq "Đang tải trang" src/pages/BankAccountsPage.tsx && echo "BankAccountsPage: no stub OK" || echo "BankAccountsPage: still has stub"
grep -vq "Đang tải trang" src/pages/CreditCardsPage.tsx && echo "CreditCardsPage: no stub OK" || echo "CreditCardsPage: still has stub"

# Pages use real components
grep -q "AccountTabs" src/pages/BankAccountsPage.tsx && echo "BankAccountsPage: AccountTabs OK" || echo "BankAccountsPage: MISSING AccountTabs"
grep -q "TransactionList" src/pages/BankAccountsPage.tsx && echo "BankAccountsPage: TransactionList OK" || echo "BankAccountsPage: MISSING TransactionList"
grep -q "FilterBar" src/pages/BankAccountsPage.tsx && echo "BankAccountsPage: FilterBar OK" || echo "BankAccountsPage: MISSING FilterBar"
grep -q "CreditCardTabs" src/pages/CreditCardsPage.tsx && echo "CreditCardsPage: CreditCardTabs OK" || echo "CreditCardsPage: MISSING CreditCardTabs"
grep -q "CreditCardTransactionList" src/pages/CreditCardsPage.tsx && echo "CreditCardsPage: CreditCardTransactionList OK" || echo "CreditCardsPage: MISSING CreditCardTransactionList"

# TypeScript compile check
npx tsc --noEmit 2>&1 | head -30
```

## must_haves

- [ ] `AccountTabs` renders shadcn Tabs driven by `useAccounts()` data, calls `setAccountId` on tab change, initializes to first account on load
- [ ] `CreditCardTabs` renders shadcn Tabs driven by `useCreditCards()` data, calls `setCardId` on tab change, initializes to first card on load
- [ ] `BankAccountsPage` composes `AccountTabs + FilterBar + TransactionList` — no stub placeholder text remains
- [ ] `CreditCardsPage` composes `CreditCardTabs + FilterBar + CreditCardTransactionList` — no stub placeholder text remains
- [ ] Switching between account tabs updates `accountId` in Zustand, which triggers a new `useInfiniteQuery` fetch for that account's transactions
- [ ] Filter state (dateFrom, dateTo, searchQuery, txType) persists when switching between accounts within BankAccountsPage
- [ ] `npx tsc --noEmit` exits with zero errors
