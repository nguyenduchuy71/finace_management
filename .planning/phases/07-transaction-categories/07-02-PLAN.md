---
phase: 07-transaction-categories
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/stores/filterStore.ts, src/components/filters/CategoryFilter.tsx, src/components/filters/FilterBar.tsx, src/features/transactions/TransactionRow.tsx, src/hooks/useTransactions.ts, src/services/accounts.ts]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
user_setup: []

must_haves:
  truths:
    - "FilterStore includes category field, defaults to 'all'"
    - "CategoryFilter component renders selector with all 6 categories + 'all' option"
    - "Changing category in CategoryFilter updates FilterStore and triggers transaction re-fetch"
    - "TransactionRow displays CategoryBadge for each transaction"
    - "Clicking CategoryBadge opens popover to override category (async pattern)"
    - "Override is persisted and applies to both display and filter"
  artifacts:
    - path: "src/stores/filterStore.ts"
      provides: "Extended FilterState with category field + setCategory action"
      exports: ["FilterState", "useFilterParams (updated selector)"]
    - path: "src/components/filters/CategoryFilter.tsx"
      provides: "Category filter control (select or radio group)"
      exports: ["CategoryFilter"]
    - path: "src/components/filters/FilterBar.tsx"
      provides: "Updated FilterBar with CategoryFilter component"
      contains: "CategoryFilter + hasActiveFilters includes category"
    - path: "src/features/transactions/TransactionRow.tsx"
      provides: "Updated TransactionRow displaying CategoryBadge + override UI"
      contains: "CategoryBadge display, click handler for override popover"
    - path: "src/hooks/useTransactions.ts"
      provides: "Updated hook with category in queryKey"
      exports: ["useTransactions (with category param)"]
    - path: "src/services/accounts.ts"
      provides: "Updated service with category in TransactionFilters interface and params"
      exports: ["TransactionFilters (with category field)"]
  key_links:
    - from: "FilterBar.CategoryFilter"
      to: "filterStore.setCategory"
      via: "onChange handler"
    - from: "filterStore.category"
      to: "useTransactions queryKey"
      via: "useFilterParams hook selector"
    - from: "useTransactions queryKey change"
      to: "transaction re-fetch"
      via: "TanStack Query auto-refetch on queryKey change"
    - from: "TransactionRow"
      to: "classifyTransaction + categoryOverrideStore"
      via: "useMemo to compute effectiveCategory"
    - from: "CategoryBadge click"
      to: "categoryOverrideStore.setOverride"
      via: "popover handler"
---

<objective>
Wire category classification into transaction rows and filtering: update stores, services, and UI components to integrate Plan 01 utilities.

Purpose: Display categories on transactions, allow override interaction, and filter by category with TanStack Query auto-refetch.

Output: Categories visible in transaction list, filter working, overrides persisted. All tests updated to expect category behavior.
</objective>

<execution_context>
@C:/Users/wtfom/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wtfom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-transaction-categories/07-RESEARCH.md
@.planning/phases/07-transaction-categories/07-01-SUMMARY.md

# Existing patterns
@src/stores/filterStore.ts (FilterState with multiple filter params)
@src/components/filters/TransactionTypeFilter.tsx (filter control pattern)
@src/hooks/useTransactions.ts (TanStack Query setup with queryKey)
@src/features/transactions/TransactionRow.tsx (transaction display pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FilterStore and FilterBar with category filter</name>
  <files>src/stores/filterStore.ts, src/components/filters/CategoryFilter.tsx, src/components/filters/FilterBar.tsx</files>
  <action>
Step 1 — Update `src/stores/filterStore.ts`:
- Add category: Category | 'all' field to FilterState interface (default: 'all')
- Add setCategory action: (category: Category | 'all') => void
- Update defaultState to include category: 'all'
- Update resetFilters() to reset category to 'all'
- Update useFilterParams() selector to include category field (use useShallow)

Step 2 — Create `src/components/filters/CategoryFilter.tsx`:
- Import: useFilterStore, Button, Popover, PopoverContent, PopoverTrigger (shadcn), Category type, CATEGORY_TAXONOMY
- Props: none (access store directly like TransactionTypeFilter)
- Render: Popover wrapper around button. Button shows current category label or "Danh mục" if 'all'
- PopoverContent: flex column with 7 radio options (6 categories + 'all')
- Each radio: onClick handler calls useFilterStore.setCategory(category)
- Style: use existing button patterns (touch-target utility, hover states)

Step 3 — Update `src/components/filters/FilterBar.tsx`:
- Import CategoryFilter from './CategoryFilter'
- Add <CategoryFilter /> component between DateRangePicker and TransactionTypeFilter
- Update hasActiveFilters to include category field: ... || category !== 'all'
- Add category to resetFilters logic (already in store, just verify it resets)
  </action>
  <verify>
    <automated>npm test -- src/components/filters/FilterBar.test.ts</automated>
  </verify>
  <done>FilterStore has category field, defaults to 'all'. CategoryFilter renders and updates store. FilterBar displays CategoryFilter. Reset clears category to 'all'. Tests for FilterBar category filter pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update TransactionRow to display and override category</name>
  <files>src/features/transactions/TransactionRow.tsx</files>
  <behavior>
    - Test 1: TransactionRow renders CategoryBadge for expense transactions
    - Test 2: TransactionRow does NOT render badge for income transactions (category applies to expenses only)
    - Test 3: CategoryBadge shows server category if no override
    - Test 4: CategoryBadge shows override category if set
    - Test 5: Clicking badge opens popover to select override
    - Test 6: Selecting category in popover calls categoryOverrideStore.setOverride and updates display
  </behavior>
  <action>
Update `src/features/transactions/TransactionRow.tsx`:
- Import: useCategoryOverrideStore, classifyTransaction, getCategoryLabel, CategoryBadge, Popover, PopoverContent, PopoverTrigger, Category from Plan 01
- Add useMemo hook to compute effectiveCategory:
  const effectiveCategory = useMemo(() => {
    if (tx.type === 'income') return null // No category for income
    const serverCategory = (tx.category as Category) || classifyTransaction(tx.merchantName)
    return useCategoryOverrideStore().getEffectiveCategory(tx.id, serverCategory)
  }, [tx.id, tx.category, tx.merchantName, tx.type])
- Inside left section (merchant name area), below date, add:
  {effectiveCategory && (
    <Popover>
      <PopoverTrigger asChild>
        <button className="mt-1"><CategoryBadge category={effectiveCategory} className="cursor-pointer" /></button>
      </PopoverTrigger>
      <PopoverContent className="w-48">
        <div className="flex flex-col gap-2">
          {Object.keys(CATEGORY_TAXONOMY).map((cat) => (
            <button key={cat} onClick={() => useCategoryOverrideStore.getState().setOverride(tx.id, cat as Category)} className="text-left text-sm p-2 rounded hover:bg-accent">
              {cat}
            </button>
          ))}
        </div>
      </PopoverContent>
    </Popover>
  )}
- Note: Do NOT render badge for income transactions (condition: tx.type === 'expense')
  </action>
  <verify>
    <automated>npm test -- src/features/transactions/TransactionRow.test.ts</automated>
  </verify>
  <done>TransactionRow displays CategoryBadge for expenses only. Clicking badge opens popover. Selecting category in popover persists override and updates display. Tests pass, no regressions in existing TransactionRow tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire category to TanStack Query and service layer</name>
  <files>src/hooks/useTransactions.ts, src/services/accounts.ts</files>
  <action>
Step 1 — Update `src/services/accounts.ts`:
- Add category?: Category field to TransactionFilters interface
- In getTransactions() function, add category param to request:
  ...(filters?.category && filters.category !== 'all' && { category: filters.category })
- Pattern matches existing dateFrom/dateTo/txType/search handling

Step 2 — Update `src/hooks/useTransactions.ts`:
- Import useFilterParams (already present)
- Extract category from useFilterParams: const { accountId, dateFrom, dateTo, searchQuery, txType, category } = useFilterParams()
- Add category to queryKey array: ['transactions', accountId, { dateFrom, dateTo, searchQuery, txType, category }]
- Add category to getTransactions call: category (in filters object)
- No other changes needed — TanStack Query auto-refetches when category changes
  </action>
  <verify>
    <automated>npm test -- src/hooks/useTransactions.test.ts</automated>
  </verify>
  <done>useTransactions hook includes category in queryKey. Category filter changes trigger transaction re-fetch. Existing tests pass. Service layer accepts category param.</done>
</task>

</tasks>

<verification>
After Task 1-3 complete:
1. Run FilterBar test: npm test -- src/components/filters/FilterBar.test.ts
2. Run TransactionRow test: npm test -- src/features/transactions/TransactionRow.test.ts
3. Run hook test: npm test -- src/hooks/useTransactions.test.ts
4. Full suite: npm test
5. No TypeScript errors: npx tsc --noEmit
6. Manual check: Open browser, filter by category, verify transaction list updates
</verification>

<success_criteria>
- FilterStore has category field, defaults to 'all'
- CategoryFilter renders popover with all 6 categories
- Changing category in CategoryFilter triggers transaction re-fetch (TanStack Query queryKey includes category)
- TransactionRow displays CategoryBadge for each expense transaction
- Clicking badge opens popover, selecting category persists override
- All tests pass: FilterBar + TransactionRow + hook tests
- No regressions in existing tests
- Category filter behavior matches other filters (date, type, search)
</success_criteria>

<output>
After completion, create `.planning/phases/07-transaction-categories/07-02-SUMMARY.md`
</output>
